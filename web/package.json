{
  "name": "restaurant-web",
  "version": "1.0.0",
  "private": true,
  "description": "Simply mexican restaurant website",
  "scripts": {
    "build": "echo \"no build\"",
    "dev": "live-server --port=8080 --entry-file=index.html",
    "start": "serve -l $PORT",
    "deploy": "railway up",
    "build:seo": "node scripts/build/sitemap.js"
  },
  "dependencies": {
    "serve": "^14.2.5"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.17.0",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}