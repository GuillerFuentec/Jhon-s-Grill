{
  "name": "restaurant-web",
  "version": "1.0.0",
  "private": true,
  "description": "Simply mexican restaurant website",
  "scripts": {
    "prepare": "node ../packages/tools/inject-boot.mjs",
    "dev": "live-server --port=8080 --entry-file=/index.html --watch=public",
    "start": "node ../packages/tools/inject-boot.mjs && serve -s public -l $PORT",
    "deploy": "railway up"
  },
  "dependencies": {
    "serve": "^14.2.5"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.17.0"
}